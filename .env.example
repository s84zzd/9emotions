# Supabase Configuration
# Get these values from your Supabase project settings at https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Note: These are public environment variables (prefixed with NEXT_PUBLIC_)
# They will be exposed to the browser. Never put sensitive keys here.
# For sensitive operations, use Supabase RLS (Row Level Security) policies
# and service role keys on the server side only.
